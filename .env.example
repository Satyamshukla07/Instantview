# Environment variables for local development

# Node Environment
NODE_ENV=development

# Server Port (default: 5000)
PORT=5000

# Session Secret (change this in production!)
SESSION_SECRET=your-secret-key-change-this-in-production

# Google OAuth Credentials (optional for local development)
# If not provided, app will auto-login as admin user
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database URL (optional - uses in-memory storage if not provided)
# Example for PostgreSQL: postgresql://user:password@localhost:5432/reelboost
DATABASE_URL=

# Domains (optional for local development)
# For local: localhost:5000
# For production: your-domain.com
REPLIT_DOMAINS=localhost:5000
